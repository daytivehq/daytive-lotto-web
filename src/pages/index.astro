---
import Layout from '../layouts/Layout.astro';
import LottoGenerator from '../components/LottoGenerator';

const today = new Date().toLocaleDateString('ko-KR', {
  year: 'numeric',
  month: 'long',
  day: 'numeric',
});
---

<Layout title="Daytive Lotto | 오늘의 로또 번호">
  <div class="flex-1 flex flex-col items-center justify-center p-4 sm:p-8">
    <div class="max-w-md w-full">
      <!-- 헤더 (정적 - SEO) -->
      <header class="text-center mb-8">
        <h1 class="text-3xl sm:text-4xl font-bold mb-2">
          오늘의 로또 번호
        </h1>
        <p class="text-gray-600 dark:text-gray-400">
          {today}
        </p>
      </header>

      <!-- 번호 생성기 (React 아일랜드 - 인터랙티브) -->
      <LottoGenerator client:load />

      <!-- 안내 텍스트 (정적 - SEO) -->
      <section class="mt-8 text-center text-sm text-gray-500 dark:text-gray-400">
        <p>매일 자정에 새로운 번호가 추천됩니다.</p>
        <p>마음에 드는 번호는 저장해두세요!</p>
        <a
          href="/favorites"
          class="inline-flex items-center gap-1 mt-3 text-blue-500 hover:text-blue-600 transition-colors"
        >
          <svg class="w-4 h-4" fill="currentColor" viewBox="0 0 24 24">
            <path d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
          </svg>
          저장한 번호 보기
        </a>
      </section>

      <!-- SEO용 숨겨진 콘텐츠 -->
      <section class="sr-only">
        <h2>로또 번호 추천 서비스</h2>
        <p>
          매일 새로운 로또 번호를 무료로 추천받으세요.
          번호는 브라우저에 저장되어 같은 날 다시 방문해도 동일한 번호를 확인할 수 있습니다.
          마음에 들지 않으면 다시 뽑기 버튼으로 새 번호를 받을 수 있습니다.
        </p>
      </section>
    </div>
  </div>
</Layout>
